flows:
  test:
    - echo: Tests starting
    - run: go test
    - echo: Tests done
  build:
    - echo: Build start
    - run: bash -c 'echo "Starting build at $(date)"'
    - run: go version
    - run: bash -c 'echo "Finishing build at $(date)"'
    - echo: Build end
  broken:
    - echo: This flow will exit with status code 1
    - run: bash -c 'exit 1'
    - echo: This step should never be reached